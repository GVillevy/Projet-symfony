{% extends 'base.html.twig' %}

{% block body %}

    <article>
        <h2>{{ news.title }}</h2>
        <div class="metadata">Ecrit le {{ news.createAt | date('d/m/y') }} à {{ news.createAt | date('H:i') }}</div>
        <div class="content">
            <img src="{{ news.image }}" alt="">
            {{ news.content | raw }}
        </div>
    </article>

{% endblock %}