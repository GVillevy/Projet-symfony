<form method="post" action="{{ path('admin_potw_delete', {'id': play_of_the_week.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ play_of_the_week.id) }}">
    <button class="btn">Delete</button>
</form>
