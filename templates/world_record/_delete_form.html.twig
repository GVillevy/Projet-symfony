<form method="post" action="{{ path('admin_world_record_delete', {'id': world_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ world_record.id) }}">
    <button class="btn">Delete</button>
</form>
